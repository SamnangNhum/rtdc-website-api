'use strict';

/**
 * question-answer-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::question-answer-category.question-answer-category');
