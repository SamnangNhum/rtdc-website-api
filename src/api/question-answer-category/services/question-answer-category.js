'use strict';

/**
 * question-answer-category service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::question-answer-category.question-answer-category');
